{
  "name": "@fbs-frontend/gulp-check-filesize",
  "version": "2.0.1",
  "description": "Logs the size of a given file and warns if a defined limit is exceeded",
  "keywords": [
    "gulpplugin, filesize, warning"
  ],
  "author": "Philipp Nowinski <philipp@sgalinski.de>",
  "repository": {
    "type": "git",
    "url": "https://github.com/Nalus/gulp-check-filesize-fork"
  },
  "scripts": {
    "test": "istanbul test _mocha --report html -- test/*.js --reporter spec",
    "coveralls": "istanbul cover ./node_modules/mocha/bin/_mocha --report lcovonly -- -R spec && cat ./coverage/lcov.info | ./node_modules/coveralls/bin/coveralls.js && rm -rf ./coverage"
  },
  "dependencies": {
    "filesize": "^3.1.2",
    "gulp-util": "~2.2.0",
    "map-stream": "0.0.5",
    "through2": "*"
  },
  "devDependencies": {
    "coveralls": "*",
    "event-stream": "*",
    "istanbul": "*",
    "mocha": "*",
    "mocha-lcov-reporter": "*",
    "should": "~2.1.0"
  },
  "engines": {
    "node": ">=0.8.0",
    "npm": ">=1.2.10"
  },
  "license": "MIT",
  "files": [
    "index.js"
  ],
  "main": "index.js",
  "directories": {
    "test": "test"
  }
}
